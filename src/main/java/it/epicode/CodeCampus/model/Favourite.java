package it.epicode.CodeCampus.model;


  /* import jakarta.persistence.*;
//import lombok.Data;

    //@Data
    @Entity
    public class Favourite {

        @Id
        @GeneratedValue (strategy = GenerationType.IDENTITY)
        private Long Id;
        @ManyToOne
        @JoinColumn(name = "user_id", nullable = false)
        private User user;

        @ManyToOne
        @JoinColumn(name = "course" +
                "_id", nullable = false)
        private Course course;

        public Favourite() {
        }

        public Favourite(User user,Course course
        ) {
            this.user = user;
            this.course
                    = Favourite.this.course
            ;
        }

        @Override
        public String toString() {
            return "Favourite{" +
                    "id=" + Id +
                    ", user=" + user +
                    ", course" +
                    "=" + course
                    +
                    '}';
        }


    }*/
